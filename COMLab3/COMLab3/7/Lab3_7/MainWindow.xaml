<Window x:Class="Lab3_7.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wfi="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"
        xmlns:local="clr-namespace:Lab3_7"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Przeglądarka WWW (wykorzystuje ActiveX WebBrowser) -->
        <WebBrowser x:Name="wbWww" Grid.Column="0" Grid.Row="0" Margin="10"/>

        <!-- PDF ActiveX host -->
        <WebBrowser x:Name="pdfHost" Grid.Column="1" Grid.Row="0" Margin="10"/>

        <!-- Odtwarzacz multimedialny -->
        <MediaElement x:Name="meAudio" Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1"
                      Height="10" LoadedBehavior="Manual" UnloadedBehavior="Stop" Margin="10" Visibility="Hidden"/>

        <StackPanel Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Otwórz WWW" Click="BtnWww_Click" Margin="5" Padding="10,5"/>
            <Button Content="Wyświetl PDF" Click="BtnPdf_Click" Margin="5" Padding="10,5"/>
            <Button Content="Odtwórz WAV" Click="BtnWav_Click" Margin="5" Padding="10,5"/>
        </StackPanel>
    </Grid>
</Window>
